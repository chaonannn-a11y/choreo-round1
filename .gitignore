# ============================================================
# GitHub Pages 部署白名单：默认忽略一切，只放行 demo 运行必需的文件。
# 目的：绝不把访谈录音/逐字稿/内部文档/原曲素材公开到全网。
# ============================================================

# 1) 先忽略所有东西
/*

# 2) 再精确放行 demo 运行必需的（! = 取消忽略）
!demo2_真分层.html
!vendor/
!beats/
!.gitignore

# 3) 放行 5 个音轨目录（分离后的音轨，demo 靠它们播放）
!stems_armageddon/
!stems_hip/
!stems_itsme/
!stems_lwymmd/
!stems_mojito/

# 4) 目录放行后，仍要挡掉里面可能的杂项
.DS_Store
**/.DS_Store

# ------------------------------------------------------------
# 注意：以下内容【被有意排除、不公开部署】——
#   访谈录音/、访谈逐字稿/、音乐素材/（原曲）、所有 *.md 文档、
#   stems/（旧 htdemucs）、_*.py 脚本、freetalk、其它 html。
# 如需部署招募帖，另行手动放行 coffee_chat_招募帖.html。
# ------------------------------------------------------------
